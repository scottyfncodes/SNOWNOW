node_modules
dist
dist-ssr
coverage
*.local
.DS_Store
.vite
*.tsbuildinfo

# Real secrets live here (GOOGLE_ROUTES_API_KEY and friends) — never commit
# this. .env.example is the template that *is* committed.
.env
.env.*
!.env.example
